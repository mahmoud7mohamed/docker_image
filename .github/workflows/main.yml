on:
  push:
    branches:
      - master


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: 'Login to Docker Hub'
        run: docker login --username ${{ secrts.DOCKER_USERNAME }} --password ${{ secrts.DOCKER_TOKEN }}